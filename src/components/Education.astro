---
import type { EducationEntry } from "@types";

export interface Props {
  education: EducationEntry[];
}

const { education } = Astro.props;
---

<section id="education" class="py-12 bg-[#0f0f0f] text-gray-300">
  <h2 class="text-2xl font-semibold text-center mb-8 text-white">🎓 Education</h2>
  <div class="max-w-3xl mx-auto space-y-6">
    {education.map(({ degree, institution, year }) => (
      <div class="border-l-4 border-[#5755ff] pl-4">
        <h3 class="text-lg font-medium text-white">{degree}</h3>
        <p class="text-sm text-gray-400">{institution} — {year}</p>
      </div>
    ))}
  </div>
</section>

