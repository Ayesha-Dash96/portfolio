---
import type { EducationEntry } from "@types";

export interface Props {
  education: EducationEntry[];
}

const { education } = Astro.props;
---

<section id="techstack" class="py-12 bg-[#0f0f0f] text-gray-300">
  <h2 class="text-2xl font-semibold text-center mb-8 text-white">🛠 Tech Stack</h2>
  <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
    {techStack.map(({ name }) => (
      <div class="px-3 py-2 rounded border border-gray-700 text-sm text-gray-300 bg-[#1a1a1a]">
        {name}
      </div>
    ))}
  </div>
</section>

